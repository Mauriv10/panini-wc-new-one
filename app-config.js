/* Central build metadata. Update this file for every published build. */
(function (scope) {
  scope.WC26_CONFIG = Object.freeze({
    version: "701.3.2",
    label: "Estabilidad y limpieza",
    cacheName: "wc26-build-701-3-2"
  });
})(typeof self !== "undefined" ? self : globalThis);
